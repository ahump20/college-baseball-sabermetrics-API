repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.24.2
    hooks:
      - id: gitleaks
        name: gitleaks (staged secrets scan)
        args:
          - --config=.gitleaks.toml
          - --redact
